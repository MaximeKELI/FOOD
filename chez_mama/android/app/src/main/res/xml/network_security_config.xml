<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Dev: allow HTTP to localhost, emulator host, and LAN IPs -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
